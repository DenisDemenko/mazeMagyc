# Публікація Maze Bridge Runner на Android

Щоб створити APK/AAB файл для Google Play, виконайте наступні кроки на своєму комп'ютері:

### 1. Встановлення залежностей
Переконайтеся, що у вас встановлено Node.js та Android Studio.

```bash
npm install
npm install @capacitor/android
```

### 2. Збірка веб-частини
```bash
npm run build
```

### 3. Ініціалізація Android платформи
```bash
npx cap add android
npx cap sync
```

### 4. Перевірка налаштувань
Файли `android/app/src/main/AndroidManifest.xml` та `strings.xml` були автоматично згенеровані мною в коді. Переконайтеся, що вони знаходяться у правильних папках. 
*Головне: в `AndroidManifest.xml` має бути рядок `android:screenOrientation="portrait"`.*

### 5. Створення іконок (Splash Screen & Icons)
Щоб замінити стандартні іконки Capacitor, встановіть інструмент генерації ресурсів:

```bash
npm install @capacitor/assets --save-dev
```
Покладіть свій логотип (1024x1024) в папку `assets/logo.png` та запустіть:
```bash
npx capacitor-assets generate --android
```

### 6. Відкриття в Android Studio
```bash
npx cap open android
```

### 7. Підписання та Реліз (Build)
1. В Android Studio перейдіть у меню: **Build** > **Generate Signed Bundle / APK**.
2. Оберіть **Android App Bundle** (для Google Play) або **APK** (для тестування).
3. Створіть новий ключ (Key Store), якщо не маєте (запам'ятайте паролі!).
4. Оберіть **Release** версію.
5. Готовий файл буде в папці `android/app/release/`.

Успіхів! 🚀